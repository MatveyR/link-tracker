app:
  telegram-token: ${TELEGRAM_TOKEN}

scrapper:
  api:
    base-url: ${SCRAPPER_API_BASE_URL:http://localhost:8081}

spring:
  application:
    name: Bot
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: ${BOT_PORT:8080}

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui


