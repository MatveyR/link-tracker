app:
  telegram-token: ${TELEGRAM_TOKEN}

spring:
  application:
    name: ${SPRING_APPLICATION_NAME}
  liquibase:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

server:
  port: ${SERVER_PORT}

springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui

scrapper:
  api:
    base-url: ${SCRAPPER_API_BASE_URL}
